This project is a fork of Kawa. It's purpose is to concentrate on the JEmacs
editor. To build using the build.scm file : 


java -classpath $CLASSPATH:lib/kawa.jar:lib/kawa-tools.jar:lib/ant.jar kawa.repl -f build.scm 